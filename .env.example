# CASCADE runs fully local on pinned public data — no API keys required.
# Optional overrides:
# CASCADE_DATA_DIR=/path/to/data           # where data/cascade/raw/ lives
# CASCADE_ALLOW_SYNTHETIC=                  # leave empty; set only inside the test suite
